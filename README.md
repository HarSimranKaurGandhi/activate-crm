
  # Interactive Quotation Management Prototype

  This is a code bundle for Interactive Quotation Management Prototype. The original project is available at https://www.figma.com/design/P6mFEMlWymbrZZXhClloSd/Interactive-Quotation-Management-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  